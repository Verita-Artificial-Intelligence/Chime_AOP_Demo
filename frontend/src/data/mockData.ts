// Types for mock data
// Data has been moved to mockData.json

// Example type definitions (add more as needed)
export type Opportunity = {
  id: string;
  name: string;
  description: string;
  customer: string;
  value: number;
  stage: string;
  closeDate: string;
  owner: string;
  notes: string;
  calls: any[];
  emails: any[];
  tags: string[];
};

// ... add other type definitions for aceRegistrations, partners, etc. as needed